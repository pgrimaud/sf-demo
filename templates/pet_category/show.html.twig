{% extends 'base.html.twig' %}

{% block title %}PetCategory{% endblock %}

{% block body %}
    <h1>PetCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pet_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pet_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pet_category_index') }}">back to list</a>

    <a href="{{ path('pet_category_edit', {'id': pet_category.id}) }}">edit</a>

    {{ include('pet_category/_delete_form.html.twig') }}
{% endblock %}
