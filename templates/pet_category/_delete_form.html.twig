<form method="post" action="{{ path('pet_category_delete', {'id': pet_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pet_category.id) }}">
    <button class="btn">Delete</button>
</form>
